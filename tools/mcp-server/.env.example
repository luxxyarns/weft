# --- Local mode (stdio) ---
# Apps configured in apps.json (see apps.json.example)

# --- Hosted mode (HTTP + OAuth 2.0) ---
# DATABASE_URL=postgresql://localhost:5432/mcp_weft
# MCP_PORT=8095
# MCP_BASE_URL=https://mcp.stash2go.com
# MCP_APP=stash2go-ii-apple

# Security: open client registration is DISABLED by default.
# Pre-register clients with: node scripts/create-client.js "client-name"
# Only set this to true for development/testing:
# MCP_ALLOW_REGISTRATION=true
