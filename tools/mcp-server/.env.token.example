# Per-user OAuth 1.0a tokens (stdio mode only)
# In HTTP mode, these come from the Bearer header instead.
# Run `npm run authorize` to obtain tokens interactively.
RAVELRY_ACCESS_TOKEN=user-access-token
RAVELRY_TOKEN_SECRET=user-token-secret
