{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://weft.dev/schemas/v1.0/profiles/ravelry/progress.schema.json",
  "title": "WEFT Progress — Ravelry Profile",
  "description": "Minimal Ravelry profile for Progress. Ravelry does not have a dedicated progress/counter model — progress is embedded in projects. This profile adds ID conventions and external_ids for linking.",
  "allOf": [
    { "$ref": "../../progress.schema.json#/$defs/ProgressState" },
    {
      "type": "object",
      "properties": {
        "id": { "type": "string", "pattern": "^[0-9]+$", "description": "Raw Ravelry project ID (progress is keyed to project)" },
        "external_ids": {
          "type": "object",
          "required": ["ravelry"],
          "properties": {
            "ravelry": { "type": "string", "pattern": "^[0-9]+$", "description": "Ravelry project ID this progress is linked to" }
          }
        }
      }
    }
  ]
}
